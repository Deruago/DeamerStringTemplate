#ifndef {{header_guard}}
#define {{header_guard}}

namespace {{language_name}} { namespace ast { namespace node {
    class {{class_name}} : public {{language_name}} {{base_names}}
    {
    private:
    public:
        {{class_name}}() = default;
        virtual ~{{class_name}}() = default;
    
    public:
        const Value* GetValue() const;
    }
}}}

#endif // {{header_guard}}